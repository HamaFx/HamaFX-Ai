name: cron-news
on:
  schedule:
    - cron: '*/5 * * * *'   # UTC, GitHub Actions 5-minute minimum granularity
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: cron-news
  cancel-in-progress: false
jobs:
  fire:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Hit /api/cron/news
        env:
          URL: ${{ secrets.PRODUCTION_URL }}
          TOKEN: ${{ secrets.CRON_SECRET }}
        run: |
          curl -fsS -X GET \
            -H "Authorization: Bearer $TOKEN" \
            "$URL/api/cron/news"
