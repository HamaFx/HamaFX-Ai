name: cron-embedding-backfill
on:
  schedule:
    - cron: '*/30 * * * *'   # UTC, GitHub Actions 5-minute minimum granularity
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: cron-embedding-backfill
  cancel-in-progress: false
jobs:
  fire:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Hit /api/cron/embedding-backfill
        env:
          URL: ${{ secrets.PRODUCTION_URL }}
          TOKEN: ${{ secrets.CRON_SECRET }}
        run: |
          curl -fsS -X GET \
            -H "Authorization: Bearer $TOKEN" \
            "$URL/api/cron/embedding-backfill"
