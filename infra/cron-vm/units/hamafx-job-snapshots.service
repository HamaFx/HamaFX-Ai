[Unit]
Description=HamaFX-Ai job — daily snapshots + candles_1m prune
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=hamafx
Group=hamafx
WorkingDirectory=/opt/hamafx/app/apps/worker
EnvironmentFile=/opt/hamafx/.env
ExecStartPre=/bin/sh -c 'echo "DEPLOYED_SHA=$(cat /opt/hamafx/.deployed-sha 2>/dev/null || echo unknown)" > /run/hamafx-job-snapshots.env'
EnvironmentFile=-/run/hamafx-job-snapshots.env
ExecStart=/usr/bin/node /opt/hamafx/app/apps/worker/dist/runner/cli.js snapshots

MemoryMax=512M
TimeoutStartSec=300

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/hamafx /run/hamafx-job-snapshots.env

StandardOutput=journal
StandardError=journal
SyslogIdentifier=hamafx-job-snapshots
